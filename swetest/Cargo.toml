[package]
name = "swetest"
version = "0.1.0"
edition = "2024"
description = "CLI test harness for the swisseph Rust ephemeris library"
license = "AGPL-3.0-or-later"

[dependencies]
bitflags = "2"
swisseph = { path = "..", package = "swisseph-rs" }
