[package]
name = "swisseph"
version = "0.1.0"
edition = "2024"

[dependencies]
bitflags = "2"
memmap2 = "0.9"

[dev-dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = { version = "1", features = ["float_roundtrip"] }
